# Branch status — `im-v3.0.0-rc1`

This branch is the active development branch for Outcome-Definition Sensitivity Analysis version `3.0.0-rc1`.

## Current phase

IM-R3 literature-collision audit, *Information & Management* journal-conversation lock and hostile statistical simulation freeze are implemented.

The branch now contains:

- the IM-R2 formal state–definition–claim framework;
- exact definition-pair decompositions and recoverability diagnostics;
- executable counterexamples and property tests;
- a literature-collision audit that narrows the novelty claim;
- an *Information & Management* journal-conversation lock;
- an ADEMP-structured simulation design frozen before full results;
- 432 primary design cells with 4,000 planned replications per cell;
- a four-stream SeedSequence execution contract;
- Monte Carlo precision and undefined-result gates;
- a protocol SHA-256 lock and regression test.

## Isolation

- base lineage: published `v2.0.2`;
- default branch `main`: not modified by RC development;
- final tag `v3.0.0`: not created;
- final GitHub release: not created;
- final Zenodo version and DOI: not created.

## Release-candidate boundary

The design is frozen but the full simulation has not been executed as manuscript evidence. Open gates include:

- full-engine compliance with four independently spawned seed streams;
- full simulation execution and hostile result audit;
- independent Study 2;
- reconstruction of the *Information & Management* manuscript;
- final article–code–table–figure crosswalk;
- institutional ethics determination and final journal declarations.

CI and smoke simulation results are engineering evidence only and must not be reported as final scientific results.

## Public-data boundary

No respondent-level data, free-text responses, direct identifiers, IP addresses, precise timestamps or paradata may be added to the version 3 public workflow.
